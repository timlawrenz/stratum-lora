# Minimal dry-run config for debugging AuraFace pipeline
# Uses batch_size=1, only 5 steps, no saving
# Run: python sdxl_train_network.py --config_file test_dryrun.toml

pretrained_model_name_or_path = "stabilityai/stable-diffusion-xl-base-1.0"
output_dir = "./output/test_dryrun"
output_name = "test"

train_data_dir = "/mnt/nas-ai-models/training-data/loras/hegre-moloko"

network_module = "networks.lora"
network_dim = 4
network_alpha = 1
network_train_unet_only = true

resolution = "512,512"
batch_size = 1
max_train_steps = 5
save_every_n_epochs = 100
save_precision = "fp16"

optimizer_type = "AdamW"
learning_rate = 1e-4

min_snr_gamma = 5.0

# AuraFace — disabled for NaN isolation
auraface_lambda = 0.0
auraface_data_dir = "/mnt/nas-ai-models/training-data/loras/hegre-moloko"
auraface_threshold = 0.3
arcface_weights = "/mnt/nas-ai-models/checkpoints/InsightFace_Pytorch+model_ir_se50.pth"

mixed_precision = "no"
cache_latents = false
cache_latents_to_disk = false
enable_bucket = true
bucket_reso_steps = 64
bucket_no_upscale = true

# Memory saving
lowram = true
vae_batch_size = 1
no_half_vae = true
